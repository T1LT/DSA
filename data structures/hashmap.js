// hash map => list/dictionary/key-value pairs

const hash = {};
const map = new Map();

// map.set(k, v)
// map.get(k)
// map.has(k)
// map.delete(k)
// map.size

// map.clear()
// map.keys() -> returns the keys in an array
// map.values() -> returns the values in an array

// class Map {
//   constructor() {
//     this.hash = {};
//     this.size = 0;
//   }

//   set(k, v) {
//     this.hash[k] = v;
//     this.size++;
//   }

//   delete(k) {
//     delete this.hash[k];
//     this.size--;
//   }
// }
